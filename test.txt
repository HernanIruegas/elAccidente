program void globalFunc;

var int : i,j;

func void uno( int a ){
	
	i = a * 2;
	if( i < a + 4 ){
		uno(a + 1);
	}
} 

func void dos( int b ){
	b = b * i + j;
}

void start{
	i = 2;
	j = i * 2 - 1;
	uno(j);

}
